#include "main.h"

/**
 * add - adds two numbers and outputs the result
 * @a: first number in the sum
 *
 * @b: second number in the sum
 *
 * Return: the sum of @a and @b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
