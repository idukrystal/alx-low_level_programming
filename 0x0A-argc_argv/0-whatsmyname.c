#include <stdio.h>

/**
 * main - entry point of program: prints program name
 * @argc: number of command line arguments
 * @argv: array of all CL arguments
 * Return: 0 on success
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
